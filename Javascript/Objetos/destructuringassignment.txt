destructuring assignment: se utiliza asignarle propiedades de un objeto a una
variable mediante la destructuración. Solo se pueden destructurar una menor 
cantidad de propiedades de las que tiene el objeto.